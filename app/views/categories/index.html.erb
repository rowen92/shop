<% @categories.each do |category| %>
  <%= link_to (image_tag category.image_url(:big)), category if category.image? %>
  <%= category.title %>
  <%= category.short_description %>
  <%= link_to "Show", category_path(category), class: 'btn btn-info' %>
<% end %>
